import type { CommanderProfile } from "../commanderProfiles.ts";

export const COMMANDER_PROFILE_REGISTRY: Record<string, CommanderProfile> = {
  helitony: {
    id: "helitony",
    preferredLanguage: "en",
  },
  helitony2: {
    id: "helitony2",
    preferredLanguage: "en",
  },
};
