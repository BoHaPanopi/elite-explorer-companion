import { useI18n } from "../i18n";
type Props = { name: string | null; onTestGreeting: () => void };
export default function AssistantPanel({ name, onTestGreeting }: Props) { const { t } = useI18n(); return <article className="panel assistant-panel"><span>{t("onboardComputer")}</span><h2>{name ?? t("unnamed")}</h2><p>{t("assistantDescription")}</p><div className="assistant-actions"><button type="button" onClick={onTestGreeting} disabled={!name}>{t("testGreeting")}</button><button type="button" disabled>{t("renameComputer")}</button></div><p className="muted">{t("renameAvailableFrom015")}</p></article>; }
