# Rank asset placeholder

Expected isolated subdirectories are `exploration/`, `exobiology/`, `trade/`, and `combat/`, with stable files for Journal levels 0 through 13 if Frontier grants permission and provides or approves a traceable source.

No badge file is currently included. The central resolver returns `null`, so the text and percentage display remains functional without an image.
